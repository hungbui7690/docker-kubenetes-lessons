/*
  Container Lifecycle
  - pic

  - run = create + start 

  - docker create hello-world
    > return <id>
  - docker start -a <id>
    > -a: print the log (output)
  - docker start <id> 
    > does not print the log
*/
